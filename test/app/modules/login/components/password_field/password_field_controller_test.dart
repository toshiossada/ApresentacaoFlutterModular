import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:apesentacaoModular/app/modules/login/components/password_field/password_field_controller.dart';
import 'package:apesentacaoModular/app/modules/login/login_module.dart';

void main() {
  initModule(LoginModule());
  // PasswordFieldController passwordfield;
  //
  setUp(() {
    //     passwordfield = LoginModule.to.get<PasswordFieldController>();
  });

  group('PasswordFieldController Test', () {
    //   test("First Test", () {
    //     expect(passwordfield, isInstanceOf<PasswordFieldController>());
    //   });

    //   test("Set Value", () {
    //     expect(passwordfield.value, equals(0));
    //     passwordfield.increment();
    //     expect(passwordfield.value, equals(1));
    //   });
  });
}
